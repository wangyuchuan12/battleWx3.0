<view style="position:fixed;z-index:2000000000000000000000;width:100%;height:100vh;background:rgba(245,250,254,0.4);"  wx:if="{{isShow}}">
  <view class="alertPlug" style="z-index:2000000000000000000000;">
    <view class="alertPlugContent">{{content}}</view>
    <button class="alertPlugButton"  open-type="getUserInfo" bindgetuserinfo="userInfoHandler" wx:if="{{type==0}}">
      确定
    </button>

    <button class="alertPlugButton"  open-type="getUserInfo" bindgetuserinfo="openSocket" wx:if="{{type==1}}">
      确定
    </button>
  </view>
</view>
